# CPU-only override: removes GPU reservation from the LLM server.
# Usage: docker compose -f docker-compose.yml -f docker-compose.cpu.yml up -d

services:
  ollama:
    deploy:
      resources:
        reservations: {}
